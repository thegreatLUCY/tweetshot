{
  "name": "tweetshot",
  "version": "1.1.0",
  "private": true,
  "description": "Image editor for the X/Twitter composer (MV3 extension)",
  "scripts": {
    "test": "node --test test/*.test.js",
    "lint": "node --check background.js && node --check content/content.js && node --check editor/editor.js && node --check lib/editor-core.js && node --check lib/editor-ui.js && node --check popup/popup.js",
    "test:browser": "bash test/harness/run.sh",
    "preflight": "node scripts/preflight.mjs",
    "build": "bash scripts/build.sh",
    "test:artifact": "bash scripts/test-artifact.sh",
    "shots": "bash scripts/shots.sh",
    "icons": "bash scripts/icons.sh"
  }
}
